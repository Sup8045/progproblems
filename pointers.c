#include <stdio.h>

int main()
{
    
    int x =4,*p = &x;
    
    printf("%d\n",*p);
    
    
    
    // * is the value of or dereferencing operator which will access the value in the location pointed by the
//pointer
    
    
    
    
}